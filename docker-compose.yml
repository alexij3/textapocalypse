services:
  db:
    image: postgres:16
    container_name: textapoc
    environment:
      POSTGRES_DB: ${PG_DB}
      POSTGRES_USER: ${PG_USER}
      POSTGRES_PASSWORD: ${PG_PASS}
      TZ: "Europe/Kyiv"
    ports:
      - "5432:5432"